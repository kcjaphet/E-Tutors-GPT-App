services:
  - type: web
    name: gpt-text-tools-api
    env: node
    region: oregon
    plan: free
    buildCommand: npm install --only=production
    startCommand: node index.js
    envVars:
      - key: NODE_ENV
        value: production
    autoDeploy: true