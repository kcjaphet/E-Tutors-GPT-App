
option_settings:
  aws:elasticbeanstalk:application:environment:
    NODE_ENV: production
    PORT: 5000
    # Don't store actual values here, use the EB Console to set these
    CORS_ORIGIN: ${CORS_ORIGIN}
